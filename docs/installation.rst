============
Installation
============

At the command line::

    $ easy_install django-slug-model-mixin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-slug-model-mixin
    $ pip install django-slug-model-mixin
